# pnpm 10+ requires explicit approval for native build scripts.
# This allows the packages we depend on to run their postinstall scripts.

# Accept the SHA-based approval that's already in package.json.
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=false
